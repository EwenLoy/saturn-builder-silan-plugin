# Saturn Builder Proxy

Minimal WebSocket proxy used by the Saturn Builder plugin.

## Environment variables

- `SATURN_BUILDER_LISTEN` (default: `127.0.0.1:8787`)
- `SATURN_BUILDER_UPSTREAM` (default: `wss://build.construct.net/`)

## Releases

GitHub Actions builds cross-platform binaries and uploads them to GitHub Releases.
